// Copy this file to runtime-config.js and replace the placeholders locally.
// runtime-config.js is ignored by Git and must be supplied by your deployment.
globalThis.METUPS_PUBLIC_CONFIG = Object.freeze({
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-supabase-anon-key',
});
